# Optional build-time GitHub token for higher API rate limits when fetching public repos.
# Fine-grained token permissions: Metadata: read-only.
GITHUB_TOKEN=

# GH_TOKEN is also supported as a fallback.
# GH_TOKEN=
